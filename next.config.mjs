/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any config you actually need below. Safe defaults here:
  experimental: {},
  images: { unoptimized: true }
};
export default nextConfig;
